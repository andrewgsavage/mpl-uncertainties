version: 2
build:
  os: ubuntu-22.04
python:
    version: 3.11
    install:
      - method: pip
        path: .
        extra_requirements:
          - doc

# Build documentation in the docs/ directory with Sphinx
sphinx:
  configuration: docs/conf.py

